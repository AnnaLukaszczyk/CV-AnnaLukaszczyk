import { StyledWrapper } from "./styled";
import { Header } from "../Header";
import { Main } from "../Main";

export const Wrapper = () => (
	<StyledWrapper>
		<Header />
		<Main />
	</StyledWrapper>
);
